---
interface Props {
    id: string;
    title: string;
}

const { id, title } = Astro.props;
---

<section id={id} class="flex flex-col gap-2">
    <h2 class="text-2xl font-extrabold">{title}</h2>
    <slot />
</section>
